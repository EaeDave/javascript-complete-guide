function sum(num1, num2) {
  const result = num1 + num2;
  return result;
  // after return statement, anything will not run.
  // there is only possible to have one return per function.
  console.log(result); // this will not run.
}
